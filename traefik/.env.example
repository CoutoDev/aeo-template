# E-mail usado pelo Let's Encrypt para avisos de expiração de certificado.
# Não é público (ao contrário do .env de cada marca) — não precisa ir pro
# build de nenhum site, só é lido pelo Traefik em runtime.
ACME_EMAIL="voce@exemplo.com"
